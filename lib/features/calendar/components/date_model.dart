import 'package:super_calendar/features/calendar/components/event_model.dart';

class DateModel {
  List<EventModel> events;
  DateTime date;
  DateModel({required this.events, required this.date});
}
